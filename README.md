# Student-File-Management-System-using-Python
The Student File Management System is a Python-based application used to manage student records efficiently using a text file (students.txt). It allows users to perform operations such as adding, viewing, searching, updating, and deleting student details like name and marks.
